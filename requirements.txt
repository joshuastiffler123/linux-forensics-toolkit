# Linux Forensics Toolkit — dependency manifest
#
# Disk forensics: Python standard library only — nothing to install.
#
# Optional — memory forensics (Volatility 3).
# Uncomment or install separately:
#   pip install volatility3
#   python linux_memory_analyzer.py --setup   # auto-clones Volatility 3
#
# volatility3>=2.0.0
